"""
每日练习 — 2026-08-23
对应课程：阶段 4 网络爬虫，第 134-136 集（阶段4完结）
今日主题：正则表达式与数据清洗
"""

# ============================================================
# 巩固练习 1：re 三函数辨析
# 涉及知识点：re.match / re.search / re.findall、Match 对象
# 简要描述：给定一段含多个手机号的文本，分别用三个函数提取，
#           对比三者返回结果与适用场景，巩固"match 看开头、
#           search 找第一、findall 找全部"的差异。
# 预计耗时：15 分钟
# ============================================================

# 要求：
# 1. 定义文本：text = "我的手机是18809090000，另一个是15500008888，备用号是13912345678"
# 2. 用 re.match 提取，观察返回（注意：text 开头不是手机号）
# 3. 用 re.search 提取第一个手机号，打印 group() 和 span()
# 4. 用 re.findall 提取全部手机号，打印返回的列表
# 5. 思考并回答：三者在爬虫清洗场景中各适合做什么？
#
# 提示：
# - pattern 统一用 r"1[3-9]\d{9}"
# - match/search 返回 None 时要先判空再 .group()
#
# 我的回答：
# （在此编写你的代码）


# ============================================================
# 巩固练习 2：分组捕获清洗时长（合并版）
# 涉及知识点：分组 ()、group(1)/group(2)、int 转换、f-string
# 简要描述：今天代码里用两次 re.search 分别提取小时和分钟，
#           请尝试用一次正则 re.search(r"(\d+)h(\d+)m", time)
#           同时提取两个分组，输出格式保持 "142m"。
# 预计耗时：15 分钟
# ============================================================

# 要求：
# 1. 定义函数 clean_duration(duration: str) -> str，带类型标注
# 2. 输入 "2h22m" 输出 "142m"；输入 "1h30m" 输出 "90m"
# 3. 用 r"(\d+)h(\d+)m" 一次匹配，group(1) 取小时、group(2) 取分钟
# 4. 匹配不到（如空串）时返回空串 ''
# 5. 思考：合并版相比今天代码里的两次 search，有什么好处？
#
# 提示：
# - hour * 60 + minute 再转 f"{total}m"
# - 记得加 r 前缀和类型标注
#
# 我的回答：
# （在此编写你的代码）


# ============================================================
# 巩固练习 3：清洗套路复用——多字段清洗
# 涉及知识点：判空 → re.search → group → 兜底空串 的清洗三套路
# 简要描述：模拟爬虫返回的"脏字典"，用今天学的清洗套路把每个
#           字段清洗干净，检验能否把套路复用到任意字段。
# 预计耗时：20 分钟
# ============================================================

# 要求：
# 1. 给定脏数据字典：
#    dirty = {
#        "年份": "(1995)",
#        "上映时间": "1995-03-10 (US)",
#        "价格": "$12.50",
#        "页数": "共320页",
#    }
# 2. 分别写出清洗函数：年份取 \d{4}、日期取 \d{4}-\d{2}-\d{2}、
#    价格取 \d+(\.\d+)?、页数取 \d+
# 3. 每个函数都遵循：先判空 → re.search 提取 → group 转字符串 → 兜底空串
# 4. 打印清洗后的干净字典
#
# 提示：
# - 价格里的 . 要转义成 \.，\d+ 取整数部分
# - 类型标注：def clean_year(raw: str) -> str:
#
# 我的回答：
# （在此编写你的代码）


# ============================================================
# 自测题
# ============================================================

# 题 1：text = "我的手机号是18809090000和15500008888"。
#        以下哪个函数能一次提取出两个手机号并返回列表？
# A. re.match(r"1[3-9]\d{9}", text)
# B. re.search(r"1[3-9]\d{9}", text)
# C. re.findall(r"1[3-9]\d{9}", text)
# D. re.split(r"1[3-9]\d{9}", text)
# 我的回答：


# 题 2：为什么正则表达式都要写成 r"..." 原始字符串？
# A. 提高匹配速度
# B. 防止字符串里的 \d、\w 等被 Python 转义，直接交给正则引擎解释
# C. r 是正则的固定前缀，不加会报语法错误
# D. 让中文在正则中也能匹配
# 我的回答：


# 题 3：在正则中，符号 ^ 的两种含义分别是什么？
#       请写出分别在什么位置时表示"开头锚点"、什么位置时表示"取反"。
# 我的回答：


# 题 4：pattern = r"(\d+)h:(\d+)m:(\d+)s"，
#       对 "10h:05m:25s" 执行 re.search(pattern, s).group(3) 返回什么？
#       如果改用 .group()（不带参数）又会返回什么？两者的区别是什么？
# 我的回答：


# 题 5：简述今天三个清洗函数（get_movie_year / get_movie_start /
#       get_movie_times）共用的"清洗三套路"是什么？
#       为什么每个函数都要在 re.search 之后判断是否匹配到再调用 .group()？
# 我的回答：


# 题 6：正则 r"\d{4}-\d{2}-\d{2}" 用于匹配日期。
#       如果遇到格式 "1994-9-23"（月份无前导零），会发生什么？
#       请说出原因，并提出一种能让它匹配的改进写法。
# 我的回答：
